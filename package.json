{
  "name": "pacetry-monorepo",
  "version": "1.0.0",
  "description": "PaceTry - 보행 속도 개인화 앱",
  "scripts": {
    "dev": "concurrently --raw \"npm run backend:dev\" \"npm run frontend:dev\"",
    "dev:tunnel": "concurrently --raw \"npm run backend:dev\" \"npm run frontend:tunnel\"",
    "dev:local": "concurrently --raw \"npm run backend:dev\" \"npm run frontend:local\"",
    "backend:dev": "cd backend && python run.py",
    "frontend:dev": "cd frontend && npx expo start",
    "frontend:tunnel": "cd frontend && npx expo start --tunnel",
    "frontend:local": "cd frontend && npx expo start --localhost",
    "backend:install": "cd backend && pip install -r requirements.txt",
    "backend:install:dev": "cd backend && pip install -r requirements-dev.txt",
    "frontend:install": "cd frontend && npm install",
    "frontend:install:dev": "cd frontend && npm install --include=dev",
    "install:all": "npm run backend:install && npm run frontend:install",
    "install:dev": "npm run backend:install:dev && npm run frontend:install:dev && npm install",
    "backend:test": "cd backend && python -m pytest",
    "backend:test:watch": "cd backend && python -m pytest --watch",
    "backend:coverage": "cd backend && python -m pytest --cov=app --cov-report=html",
    "lint:backend": "cd backend && python -m flake8 app/",
    "lint:frontend": "cd frontend && npx expo lint",
    "lint:all": "npm run lint:backend && npm run lint:frontend",
    "format:backend": "cd backend && python -m black app/ tests/ && python -m isort app/ tests/",
    "format:frontend": "cd frontend && npx prettier --write \"**/*.{js,jsx,ts,tsx,json}\"",
    "format:all": "npm run format:backend && npm run format:frontend",
    "type-check:backend": "cd backend && python -m mypy app/",
    "type-check:frontend": "cd frontend && npx tsc --noEmit",
    "type-check:all": "npm run type-check:backend && npm run type-check:frontend",
    "security:backend": "cd backend && python -m bandit -r app/",
    "test:all": "npm run backend:test",
    "backend:docs": "echo Opening API docs... && start http://localhost:8000/docs",
    "clean:backend": "cd backend && (rmdir /s /q __pycache__ 2>nul || rm -rf __pycache__ 2>/dev/null || echo Cache cleaned)",
    "clean:frontend": "cd frontend && (rmdir /s /q .expo node_modules 2>nul || rm -rf .expo node_modules 2>/dev/null || echo Cache cleaned)",
    "clean:all": "npm run clean:backend && npm run clean:frontend",
    "setup": "npm run install:all && npm run backend:install:dev && npm run frontend:install:dev",
    "start": "npm run dev",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "release": "git checkout main && git pull origin main && npm version patch && git push origin main --tags",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "~5.9.2"
  },
  "keywords": [
    "react-native",
    "fastapi",
    "walking",
    "personalization",
    "expo",
    "python"
  ],
  "author": "PaceTry Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/pacetry.git"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@react-native-community/netinfo": "^11.4.1",
    "expo": "^54.0.12",
    "react": "19.1.0",
    "react-native": "0.81.4"
  }
}
